# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/

# Native
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# Debug
npm-debug.*
yarn-debug.*
yarn-error.*

# OS
.DS_Store
Thumbs.db
desktop.ini
*.pem

# Local env — never commit real secrets
.env
.env.*.local
.env.local

# TypeScript (future)
*.tsbuildinfo

# Legacy static Expo config — replaced by app.config.js. Kept ignored so a
# stray regeneration doesn't re-introduce the old hardcoded TMDB key.
app.json

# AI scratch notes
GEMINI.md
CLAUDE.md.local

# IDE
.idea/
*.swp
